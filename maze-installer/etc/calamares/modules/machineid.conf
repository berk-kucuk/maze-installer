# Maze Linux — machine-id / entropy.
#
# unpackfs copies the live system's /etc/machine-id and D-Bus id; regenerate them
# so every installed machine gets a unique identity.
---
systemd: true
dbus: true
dbus-symlink: true
entropy-files: false
