# Maze Linux — removeuser module.
#
# unpackfs copies the WHOLE live airootfs onto the target, including the live
# 'maze' user (passwordless, autologin). Drop that account before the real user
# is created. deploy-to-target.sh additionally removes the leftover /home/maze
# and the live-only autologin / NOPASSWD-sudo / pkexec rules.
---
username: maze
