# Maze Linux — locale / timezone defaults.
#
# Initial map selection only; the user changes it on the Locale page. No geoip
# lookup (offline-friendly), so set a sane default region/zone.
---
region: "Europe"
zone:   "Istanbul"
